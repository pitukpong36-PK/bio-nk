# ✅ เช็กลิสต์ก่อนเผยแพร่ให้นักเรียนใช้จริง

> อัปเดตล่าสุด: 11 ก.ย. 2569 · อ้างอิงแนวทางเดียวกับโปรเจกต์ PKchemistry

---

## สถานะปัจจุบัน

| รายการ | สถานะ |
|---|---|
| แยกไฟล์ + โหลดเร็ว (เฟส 2) | ✅ หน้าแรก 221 KB |
| โหมดมืด · มือถือ · เข้าถึงได้ (เฟส 3) | ✅ ผ่าน WCAG AA |
| แบบทดสอบ + แดชบอร์ด (เฟส 4) | ✅ 544 ข้อ |
| PWA (ติดตั้งเป็นแอป/ออฟไลน์) | ✅ ทดสอบผ่านแล้ว |
| git + GitHub | ✅ https://github.com/pitukpong36-PK/bio-nk (public) |
| **เว็บออนไลน์** | ✅ **https://pitukpong36-pk.github.io/bio-nk/** (11 ก.ย. 2569) |
| โค้ดระบบสมาชิก | ✅ เขียนเสร็จแล้ว รอใส่ค่า Firebase |
| **เปิดใช้ Firebase จริง** | ⬜ **ยังไม่ทำ** — ตอนนี้ทำงานโหมดเก็บในเครื่อง |

---

## 🔴 ขั้นตอนขึ้นเว็บจริง

### 1. ~~สร้าง repo และ push~~ ✅ ทำแล้ว

repo อยู่ที่ https://github.com/pitukpong36-PK/bio-nk — ตอนนี้เป็น **private**

### 2. ~~เปิด GitHub Pages~~ ✅ ทำแล้ว

**เว็บออนไลน์แล้วที่ https://pitukpong36-pk.github.io/bio-nk/**

รอ 1–2 นาที เว็บจะขึ้นที่
**https://pitukpong36-pk.github.io/bio-nk/**

### 3. ตรวจหลังขึ้นเว็บ ✅ ทดสอบอัตโนมัติผ่าน 33/33

- [x] เนื้อหาบทโหลดครบ (ทดสอบเล่ม 1, 4, 6)
- [x] แบบทดสอบ 544 ข้อ ทำงาน · บันทึกผลได้
- [x] service worker ลงทะเบียน · แคช 28 ไฟล์ · ดึงหน้าจากแคชได้
- [x] manifest + ไอคอนครบ (ติดตั้งเป็นแอปได้)
- [x] โหมดมืด · ฟอนต์ในเครื่อง · ค้นหา
- [x] โหลดจริง 51.5 KB · เห็นเนื้อหาใน 84 ms
- [ ] **ยังต้องลองบนมือถือจริงของตัวเอง** (เครื่องจริงต่างจากทดสอบอัตโนมัติ)

### 4. แจกให้นักเรียน

- [x] QR code พร้อมแล้วที่โฟลเดอร์ **`แจกนักเรียน/`** (PNG + SVG)
- [ ] ลองให้นักเรียน 2–3 คนใช้ก่อน ดูว่าติดตรงไหน

---

## ⚠️ กฎเหล็กหลังขึ้นเว็บแล้ว

### แก้เนื้อหาทุกครั้ง ต้องอัปเลขเวอร์ชันแคช

เปิด `sw.js` บรรทัดที่ 8 เปลี่ยนเลขขึ้นทีละหนึ่ง:
```js
const CACHE = 'bio-nk-v1';   →   const CACHE = 'bio-nk-v2';
```
**ไม่อัป = นักเรียนที่เคยเปิดเว็บแล้วจะเห็นของเก่าค้าง**

### แก้โจทย์หรือหัวข้อ ต้องรัน build ก่อน push
```bash
python3 _build/build.py
```

### ลำดับที่ถูกต้องเวลาจะอัปเดตเว็บ
```bash
# 1. แก้เนื้อหาใน data/vol*.js
# 2. สร้างไฟล์ข้อมูลใหม่
python3 _build/build.py
# 3. อัปเลขเวอร์ชันใน sw.js
# 4. ส่งขึ้นเว็บ
git add -A && git commit -m "อธิบายสั้น ๆ ว่าแก้อะไร" && git push
# 5. รอ 1-2 นาที
```

### ถ้าแก้ผ่านหน้าเว็บ GitHub (จากมือถือได้)
แก้ไฟล์ → Commit changes → **อย่าลืมแก้ `sw.js` ด้วย**
แล้วเวลากลับมาแก้ในเครื่อง ต้อง `git pull` ก่อนเสมอ ไม่งั้นทับงานที่แก้บนเว็บ

---

## 🟡 ระบบสมาชิก (ทำทีหลังได้)

ตอนนี้ความก้าวหน้าและผลทบทวน **เก็บในเบราว์เซอร์เครื่องที่ใช้เท่านั้น**
- เปลี่ยนเครื่อง/ล้างข้อมูลเบราว์เซอร์ = หาย
- ครูดูภาพรวมทั้งห้องไม่ได้

ระหว่างนี้ใช้ทางนี้ไปก่อนได้: หน้า **ความก้าวหน้า** → ปุ่ม **ส่งออก CSV** ให้นักเรียนส่งไฟล์มา
และปุ่ม **สำรองข้อมูล (JSON)** สำหรับย้ายเครื่อง

**โค้ดเขียนเสร็จแล้ว** (`js/cloud.js` + `js/account.js`) รอแค่ใส่ค่าจริง 5 ขั้นตอน:

**1. สร้างโปรเจกต์ Firebase**
- ไปที่ https://console.firebase.google.com → Add project (ชื่ออะไรก็ได้ เช่น `bio-nk`)
- ปิด Google Analytics ได้ ไม่จำเป็น

**2. เปิด Authentication**
- เมนู Build → Authentication → Get started
- แท็บ **Sign-in method** → เปิด **Email/Password**
- แท็บ **Settings → Authorized domains** → เพิ่ม `pitukpong36-pk.github.io`

**3. เปิด Firestore**
- Build → Firestore Database → Create database
- เลือก **Production mode**
- Location: **asia-southeast1 (Singapore)** ← สำคัญ เร็วกว่าสำหรับผู้ใช้ในไทย

**4. ใส่ค่า config ลงในโค้ด**
- Project settings (⚙️) → เลื่อนลงหา "Your apps" → กดไอคอน `</>` (Web)
- คัดลอกค่าใน `firebaseConfig` มาวางทับใน **`js/cloud.js` บรรทัด 13–20**
- พอ `apiKey` ไม่ขึ้นต้นด้วย `YOUR_` ระบบจะสลับเป็นโหมดคลาวด์เองอัตโนมัติ

**5. ตั้ง Security Rules**
- Firestore → แท็บ **Rules** → ลบของเดิมทิ้ง
- คัดลอกเนื้อหาทั้งหมดจากไฟล์ **`firestore.rules`** ในโปรเจกต์นี้ไปวาง → กด **Publish**
- ⚠️ ไม่ทำขั้นนี้ = ใครก็อ่านข้อมูลนักเรียนได้ หรือเขียนอะไรไม่ได้เลย

**ตรวจหลังเปิดใช้:**
- [ ] สมัครบัญชีทดสอบ → ดูว่าข้อมูลขึ้นใน Firestore → users
- [ ] ล็อกอินด้วยอีเมลครู (`pitukpong36@brw.ac.th`) → เห็นปุ่ม "แดชบอร์ดครู"
- [ ] ล็อกอินบัญชีนักเรียนคนละเครื่อง → ความก้าวหน้าตามมาจริง
- [ ] ลองบัญชีนักเรียนกดเข้าแดชบอร์ดครู → ต้องเข้าไม่ได้

**เปลี่ยนรายชื่อครู** ต้องแก้ 2 ที่ให้ตรงกัน:
- `TEACHERS` ใน `js/cloud.js`
- `isTeacher()` ใน `firestore.rules` (แล้ว Publish ใหม่ในคอนโซล)

---

## 🟢 หมายเหตุ

- ไฟล์ `.nojekyll` ต้องมี ไม่งั้น GitHub Pages จะข้ามโฟลเดอร์ที่ขึ้นต้นด้วย `_`
- `_archive/` ถูกกันไม่ให้ขึ้นเว็บแล้ว (อยู่ใน `.gitignore`)
- รูปทั้งหมดเป็น inline SVG วาดเอง + ฟอนต์ OFL → เผยแพร่สาธารณะได้ ไม่ติดลิขสิทธิ์
- เนื้อหาบท 19–29 ยังเป็นสรุปย่อและไม่มีโจทย์ — เผยแพร่ได้ แต่ควรบอกนักเรียนว่ากำลังทยอยเพิ่ม
